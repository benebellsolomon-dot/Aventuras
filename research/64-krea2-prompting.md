# 64 — Krea 2 Turbo prompting: research, measurements, decisions

**Date:** 2026-08-21 · **Parent:** research/63 (D5 round 1b image-model switch) · **Live profile:** NanoGPT `wavespeed-ai/krea-v2/turbo-lora` + Nikke style / Sagging / NSFW MASTER LoRAs via Civitai token URLs (verified working), style preset `image-style-nikke-krea2`.

## 1. What Krea 2 Turbo is (sourced)

- **Text encoder = Qwen3-VL-4B-Instruct**, 12-layer hidden-state tap (`hidden_states[2,5,…,35]`, `(B,12,seq,2560)`) fused inside the DiT by a text-fusion adapter; single-stream DiT, 12.9B, 28 blocks, 3-axis RoPE. Sources: Krea 2 technical report (krea.ai/blog), `krea/Krea-2-Turbo` model card, ComfyUI `comfy/text_encoders/krea2.py`.
- **The encoder is prompted with the Qwen-Image system template:** *"Describe the image by detailing the color, shape, size, texture, quantity, text, spatial relationships of the objects and background:"* — i.e. the model literally reads your prompt as an LLM's description of the finished image. Dense descriptive prose with colors/sizes/quantities/spatial relations is the native format. (ComfyUI `krea2.py`, `KREA2_TEMPLATE`.)
- **No token cap in the reference tokenizer** (`max_length=99999999` in ComfyUI's Qwen3-VL tokenizer); practical limit is attention dilution, not truncation. Community note: reference *images* cost ~1k vision tokens and can starve text — irrelevant to our txt2img path.
- **No negative prompt.** No CFG on Turbo (distilled; `guidance_scale≈0`, **8 steps**). `(word:1.3)` weighting does not isolate a concept — **order is the emphasis lever; the encoder front-loads**. (fal.ai guides, comfy.icu node docs, krea-explorations.)
- **Style:** name the medium/technique concretely and put it last; quality boilerplate ("masterpiece, 8k, best quality") is ignored or harmful. In-image text goes in double quotes. (krea-ai/krea-2 `docs/prompting.md`, fal.ai, InstaSD.)
- **Censorship:** community teardowns place the safety tuning in DiT layers 9–10 and say it damps not only nudity but strong expressions, injury, fear, anger (MyAIForce). Local workflows patch the DiT (Krea2T Enhancer / bypass LoRAs) AND swap an *abliterated* Qwen3-VL encoder. **On NanoGPT we have neither patch** — only LoRAs (NSFW MASTER, NSFW v3) on the diffusion side; the stock encoder still reads the text. Official LoRA guidance: train on Raw, run on Turbo; scale 1.0–1.25 for style, up to 1.5–2.0 for uncensor LoRAs; stack ≤3; rare trigger words used identically. Official LoRA trainer captions: describe literal content only, NO style/medium/lighting-mood words (matches the Nikke LoRA author's "avoid stylizers").
- **Krea's own `expansion.txt`** (the LLM prompt-expander): preserve the user's subjects/actions/colors/spatial arrangement, group each subject with its attributes, quote exact text, honor a requested medium, don't over-specify, output ONE unified paragraph. The Civitai Krea2 workflow's enhancer system prompt (extracted from the workflow JSON subgraph) prescribes: subject (ethnicity+age first, body proportions, face/skin/expression/gaze, hair, accessories, clothing materials/fit) → pose & composition (pose, hands, framing, camera angle, placement, **character dynamics incl. explicit acts**) → environment (location, background, depth of field) → lighting (direction/softness/contrast, time of day) → style & technical (medium, lens look, sharpness) → color & mood — "a single, highly descriptive paragraph of natural, dense prose; concrete, reproducible descriptors".

## 2. Measured here (NanoGPT `krea-v2/turbo`, 1k, two renders)

1. **Long-prompt retention:** a ~1,450-char prompt (≈300 tokens) with five distinctive objects placed at the very END — brass telescope on a tripod right of the window, red paper lantern on the center rafter, black cat on a blue velvet cushion at left, open journal with quill, chessboard beside the oil lamp — rendered **every one**, plus rain on the dormer, lamp, trunks, rug, off-shoulder shirt, mug. Misses: lantern lit (prompt: unlit), gold eyes → grey, "flour on one cheek" absent. **Conclusion: no effective truncation at ~1.5k chars; our 400–700/900 ceilings were leaving detail on the table.**
2. **Two characters with per-person sentences + spatial anchors** ("on the left, …; on the right, …"): every trait stayed on its owner (red wavy hair + green gambeson + brass buckles + boots left; black bob + olive skin + leather halter + grey trousers right; firelit kitchen door behind; wet flagstones). Misses: "fierce grin" rendered as a scowl (consistent with the expression-damping report), "reverse grip" ignored, one extra blade. **Conclusion: the anchored-sentence protocol works; emotion needs to be stated concretely and early.**

## 3. What changed in the app (this commit)

- **Narration-side prose `<pic>` instructions** (`NarrativeService.ts` `INLINE_IMAGE_INSTRUCTIONS_PROSE`, injected as `{{ inlineImageInstructions }}` — code, no pack sync): explains how the encoder reads the prompt; ONE dense paragraph; **count + subjects FIRST** (front-loaded emphasis), then action/interaction, then camera, then environment + light (style appended automatically); no quality words / weights / negatives; quoted in-image text; concrete emotion; anchored per-person sentences kept; breast-band words kept (the BE size-grounding rewrites them and the LLM encoder understands them); **the booru-style rating prefix ("general" / "sensitive" / "explicit, uncensored, detailed anatomy") is gone** — replaced by CONTENT DISCIPLINE: scope clothing/nudity/acts to THIS beat, and when the beat is explicit describe it literally and anatomically in full sentences, no euphemism/slang, no rating labels ("uncensored" is not a picture word and the stock encoder is the one reading it). Density 600–1000 (one character) / 900–1400 (several), hard cap 1600.
- **`image-prompt-analysis` service template** (post-hoc scene path): same encoder note, subject-first order (camera moved after action; style LAST), 600–1000 / max 1400, concrete-emotion and quoted-text rules, beat-scoped content rule 9. **Service template sync v12.**
- Provider side was already right for Turbo: prose models get no `guidance_scale`/`num_inference_steps` (NanoGPT/wavespeed defaults = Turbo's 8-step no-CFG), no negative prompt, `n: 1`, LoRAs pass through.

## 3b. Option B shipped — rating-based image routing (Ben's ruling)

- **Rating signal:** the `<pic>` tag gained a REQUIRED `rating="general|sensitive|explicit"` attribute in BOTH dialect instruction blocks (an attribute, never prompt words — the prose rating prefix stays gone); `extractPicTags` parses it tolerantly (`parseBeatRating`: case/aliases — nsfw/adult → explicit, suggestive/questionable → sensitive) and falls back to a booru rating tag opening the prompt. Analyzed scenes carry a tolerant `rating` field (`imageableSceneSchema`, both analysis templates ask for it; **sync v13**).
- **Routing:** `image/ratingRouting.ts` `resolveRatingRoute(rating, imageSettings)` → the **Explicit-Beat Profile** (`imageGeneration.explicitProfileId`, size `explicitSize`, falls back to the primary size) when the beat is `explicit` and a profile is set; otherwise the primary. Applied in all three generation paths (streaming tracker, post-hoc inline service, analyzed scenes) right after the primary pick and BEFORE the portrait-reference override — a routed beat skips img2img references (identity rides the booru banks on that path); portraits are never routed. Because the booru writer and dialect are keyed on the resolved MODEL, an explicit beat routed to an Illustrious profile automatically gets the full booru pipeline (writer, tag budget, negatives, size hoist) while everything else stays on Krea prose.
- **Settings UI:** Settings → Images → General: "Explicit-Beat Profile (optional)" + "Explicit Image Size" (+ Clear). Recommended: a second NanoGPT image profile on `wai-illustrious-sdxl` (the model the booru writer was calibrated on; `nsfw: true` on NanoGPT), size 832x1216.
- **Fallback (Ben, live: narrators omit or under-set the attribute):** the routed rating is `effectiveBeatRating(declared, prompt)` = the HIGHER of the declared rating and `inferRatingFromText(prompt)` (word-bounded explicit vocabulary — nudity/anatomy/acts in both dialects; suggestive words only reach `sensitive`). Text can upgrade, never downgrade: a false upgrade sends a suggestive beat to the explicit profile, a miss renders a doll. Applied in the `<pic>` parser and the analyzed-scene path.
- **Tests:** `ratingRouting.test.ts`, `inlineImageParser.test.ts`, a tracker routing case (explicit tag → explicit profile + its model reaches the booru writer; general tag → primary). Suite 1372 → 1380.

## 3c. Why the first live Krea prompts were still booru tags — and the two fixes (shipped)

**Observed (Ben):** a Krea render whose prompt was `sensitive, medium close-up, slightly low angle, 1girl, solo, … (E-cup), … depth of field. Nikke style anime. …` — booru tags with the prose style block appended, rendered poorly. **Cause:** in inline-image mode the narrator's history keeps every earlier `<pic>` tag verbatim (`NarrativeService.buildUserPrompt`), and this story carried ~10 tag-dialect tags from its Illustrious days — in-context examples beat the new instructions, and those old tags have no `rating` (hence the routing misses).
1. **History hygiene:** `stripForeignDialectPicTags(content, dialect)` (`inlineImageParser.ts`, with `looksBooruPrompt`: rating-tag opener, count tags, or a dense comma list of ≤3-word fragments) — the narrator's history now keeps only the CURRENT image dialect's tags; the prose instructions also say outright not to imitate older tag-format `<pic>` tags.
2. **Dedicated prose scene writer** (`image/prosePromptWriter.ts`, template `image-prose-scene-prompt`, **sync v14**, setting `dedicatedProsePromptWriter` default on, same `imageGeneration` preset as the booru writer): Krea's own recommended two-stage pipeline. For prose-dialect models every `<pic>`/scene intent — prose OR leftover tags — is rewritten into one dense subject-first paragraph (600–1400 chars, capped 1600 on a sentence boundary) from the subjects' descriptors + current clothing + the engine's BE body/expression state + story setting + POV + current location, under the Krea rules (no quality/style words, no tag lists, concrete emotion, anchored per-person sentences, beat-scoped literal content discipline, no character names). Best-effort: any failure keeps the original prompt. Wired through a single dispatcher `scenePromptWriter.resolveScenePrompt` (booru writer → prose writer; each a no-op for the other dialect) in all three seams. Cost: one extra LLM call per image on the image-prompt preset.
- Tests: parser dialect classification/stripping, prose dossier/cap/gate; tracker test routes the dispatcher to its booru mock. Suite 1384 → 1390.

**Live check after 3c (DB):** routed explicit images now land on `wai-illustrious-sdxl` — but with the booru quality prefix in front of a PROSE paragraph: Ben's settings had `dedicatedBooruPromptWriter: false` (Settings → Experimental), so the tag converter never ran on the routed beats. Fix: turn it on; the Experimental tab now warns when an Explicit-Beat Profile is set with the booru writer off, and has a sibling toggle for the prose writer.

## 3d. First routed NSFW beat on WAI Illustrious: bad anatomy — cause and fix (shipped)

The converter ran (booru writer ON) and produced: `…explicit, uncensored, detailed anatomy, medium shot, pov, 1boy, 1girl, hetero, vaginal, standing sex, standing, pressed together, arms around neck, vaginal from behind, impaled, medium breasts, male pov, faceless male, muscular, partially dressed, long hair, … topless, blush, attic, dusty, wooden beams, heavy breathing` (~45 tags, many multi-word). Two defects: (1) **mixed arrangements** — face-to-face (`pressed together, arms around neck`) AND from-behind (`vaginal from behind`) in one action block → two poses at once → tangled anatomy; (2) **over the 77-token window** — the single-window budget counted TAGS (36), but multi-word tags cost 3–4 tokens each, so the tail (environment, possibly part of the identity run) was truncated on NanoGPT. Fixes in `booruPromptWriter.ts`: a third mechanical defect `conflictingArrangement` (FACING vs BEHIND tag sets) joins the act check → same single corrective retry, then `resolveArrangementConflict` drops the minority family deterministically (keeps the writer's lead); and a **token-estimated post-pass** for single-window endpoints (`BOORU_SINGLE_WINDOW_TOKEN_BUDGET` 66 ≈ 77 − quality prefix − EOS; `estimateTagTokens` = words (+1 for long words) + comma) trimming scene tail → expressions → action → run tails (identity core ≥ 6 kept). Tests +4 (suite 1394). Note: the image-prompt preset is now `deepseek-v4-pro`; the booru writer was calibrated on kimi-k3 in round 1 and deepseek produced the mixed arrangement — if contradictions keep appearing in the dev log (`act-reliability validation failed … conflictingArrangement`), move the image-prompt preset back to kimi-k3.

**Live (Ben, 2026-08-22):** image-prompt preset switched back to `moonshotai/kimi-k3` → noticeably better booru-writer adherence on the routed explicit beats. Session closed here; state below is the handoff.

## 3e. Round 2, first pass (2026-08-22) — dress-state backstop, prose identity anchors, Krea size knobs

Evidence was pulled read-only from the live DB (`embedded_images` + the owning `story_entries`) rather than from the dev log:

- **Dress-state failure, twice.** 03:17Z: narrator `<pic … rating="explicit">` said *"standing bare and pressed against a man, her hand on his belt buckle"* and the narrative said *"her bare chest presses against yours, skin on skin"*; the booru writer emitted `… clothed, long hair, …, damp halter top, partially undone …` — drawn clothed. 17:44Z: the pic said *"sitting bare on a wooden crate, legs open in invitation, hands braced, looking up"*; the writer emitted `explicit, uncensored, detailed anatomy, medium shot, pov, from above, 1boy, 1girl, hetero, sitting, medium breasts, [POV run], [identity run], blush, dusty attic …` — no dress state, no pose, an explicit rating with nothing to draw. Cause: the template's *"copy the dossier's CURRENT CLOTHING verbatim"* rule (what she wore when the beat began) beats the beat, and both were ANTICIPATION beats (`actInProgress: false`) so the act backstop never ran.
  **Shipped:** `image/dressState.ts` — `inferImpliedDressState(intent)` (nude / topless / bottomless from the narrator's scene intent only; conservative: bare shoulders/feet/thighs/hands, idioms, "half-undressed" never trigger) + the Danbooru dress-state vocabulary (`DRESS_STATE_TAGS`: nude family + clothes-displaced family). Booru writer: `detectMissingDressState` (intent says bare, no described run states ANY dress state; faceless-POV run excluded) merged into `detectWriterDefects` so the single corrective retry carries every note; after the retry `applyDressStateFallback` undresses the SINGLE named subject mechanically (her run by bank overlap, drops `clothed`, appends `completely nude`/`topless`/`bottomless`; several named subjects = ambiguous = no change). Template (sync **v15**): per-run DRESS STATE rule in the `characters` field + the clothing rule rewritten ("the beat wins"); schema `characters` description matches. Dev-log signature to watch: `act-reliability validation failed {missingDressState: true}` and `dress-state fallback applied`.
- **Prose (Krea) path polish.** Dossier now leads with `identity anchors (locked — restate every one in plain words): <bank>` when the character has an `imageTags` bank (the descriptor line was the only identity source, and descriptors carry transient pollution); the clothing line is labelled as what she wore as the beat began. Template gains IDENTITY ANCHORS (restate hair/eye/skin/marks every picture; dossier wins over intent on those) and EXPRESSION TWICE (state it in her sentence, restate before the environment) rules, and the same dress-state wording as the booru side.
- **Krea size knobs — measured.** Decoding the stored renders: a 1536x1536 request rendered **1024x1024** on `wavespeed-ai/krea-v2/turbo` and **832x1248** on `turbo-lora` (the portrait reference's 2:3 — the non-routed path is img2img at wavespeed's default `strength 0.65`, which also means the scene is ~35 % anchored to the portrait composition; note for round 2b). NanoGPT's model metadata lists `aspect_ratio` (fixed W:H option list) and `resolution` (`1k`/`2k` on the turbo family; the aspect string itself on `krea-v2-large/medium/medium-turbo`) — width/height are ignored. **Shipped:** `wavespeedKreaSizeParams(model, w, h)` in `providers/nanogpt.ts` — nearest listed aspect by log-ratio, `2k` at ≥ 2 MP requested (`KREA_TWO_K_MIN_PIXELS`), sent alongside width/height for the wavespeed Krea ids only. Unverified live until the next Krea render; expected: 1536x1536 → `1:1` @ `2k` (≈2048², $0.025 instead of $0.015 on turbo-lora).
- **Classifier re-verify (§4.3):** 22 classified turns since the entity-guard fix, **0** `_guardRejects` in the live DB on kimi-k3 → routing healthy; nothing to tune.

Suite 1416, check/lint clean.

## Live configuration at session close (2026-08-22)

- Branch `claude/opus-agents-orchestration-82d727` @ this doc's commit, pushed. Suite 1394, check/lint clean.
- Presets: classification `kimi-k3` (NanoGPT), suggestions/action-choices `deepseek-v4-pro`, image-prompt (booru + prose writers, scene analysis) `kimi-k3`, narrative Kimi K3. NanoGPT is prompt-schema-only for structured output (`structuredOutputUnenforced`).
- Images: primary profile NanoGPT `wavespeed-ai/krea-v2/turbo-lora` + LoRAs (Nikke style / Sagging / NSFW MASTER via Civitai token URLs), style `image-style-nikke-krea2`, size 1536x1536; **Explicit-Beat Profile** "NanoNSFW" = NanoGPT `wai-illustrious-sdxl` (1024x1024, booru aspect picker overrides); booru writer ON, prose writer ON; template sync v15 (was v14 at the 2026-08-22 close).
- Routing: `<pic rating>` attribute + text-inferred upgrade → explicit beats to WAI through the booru writer (act check + arrangement check + token budget); everything else Krea through the prose writer.

## 4. Open (round 2 — next session starts here, in priority order)

1. **NSFW quality on WAI (routed beats):** dress-state backstop SHIPPED (§3e) — verify on the next routed beats (`missingDressState` / `dress-state fallback applied` in the dev log; healthy = the writer states the dress state first time). Still open: pose tags from the intent (the 17:44Z beat lost "spread legs", "looking up"); tune FACING/BEHIND sets and the token budget (66) against real renders; `explicitSize` is already 832x1216.
2. **Krea prose path polish:** identity anchors + expression-twice + dress wording SHIPPED (§3e); `aspect_ratio`/`resolution` SHIPPED, verify the first render's actual dimensions. Open: the non-routed Krea path is img2img on the portrait at wavespeed default strength 0.65 (inherits its aspect, anchors composition) — decide whether to expose `providerOptions.strength` or drop the reference for scene beats; portrait (`image-portrait-generation`) and background templates to the same density/ordering rules.
3. **D5 knobs (research/63):** chekhov cooldown/thresholds, rel pacing, world-sim gate frequencies, DC-chip tagging rate (prompt nudge drafted in research/63), arousal-suppression threshold, narrative short-term-memory vs provider config. Classifier routing on kimi RE-VERIFIED (§3e: 0 guard rejects / 22 turns).
4. **Deferred FF5.2:** E5 notebook / E6 NPC thoughts / E7 titles (research/58).


## 5. Archive — the NSFW-on-NanoGPT-Krea investigation log (resolved by §3b routing; kept for the evidence)

1. **NSFW on the stock encoder** — Ben testing. **Round-2 result so far: NSFW MASTER alone at 1.0 / 1.5 / 2.0 FAILS** (scale on a content LoRA does not beat the layer-9/10 filter). Next rungs, from the MyAIForce six-method comparison + Civitai metadata:
   - **Bypass LoRAs (plain files, usable via `loras`):** `Krea2FilterBypass` by S1LV3RC01N (model 2728234) — 2-vector version **3066812** (layers 9–10, "moderate improvement, stable") and 3-vector **3067151** (9–11, stronger but unstable: missing characters / clothing artifacts); author: strength "1.0 all the way up to 40000 — almost impossible to overdo" (black images only on badly quantized models). `Krea2 Filter Bypass [Fedor]` (model 2746817, version **3089754**, ~1 MB, layers 9–10 only) — "strongest overall performer, improved realism, struggles more with explicit content". `skc3vo` (all 12 layers @ ~0.01) — plastic skin, not recommended.
   - **NSFW LoRAs:** `Krea 2 NSFW` by 19doorsside884 (model 2725430): **v3 = version 3071760** (218 MB, strength 1–1.5, the Civitai workflow's pick), v4.1 = 3085473, v4.3_EXP = 3147117 (436 MB, realism-tuned); `NSFW MASTER Krea2_Turbo` = 3232953 (1.8 GB, failed alone). Author's wording tip: write the person first, then the body part ("a man …, his cock …"), not the part alone — subject-first, as our `<pic>` rules already require. Author also recommends the `Beinsezii/Krea-2-Turbo-Projector-Scale-LoRA-Diffusers` (HF) at 0.01 for adherence (costs a slot).
   - **Recommended stacks (3 slots):** A = FilterBypass 2-vector (3066812) @5 (→20) + NSFW v3 (3071760) @1.5 + Nikke @0.9; B = Fedor (3089754) @1.0 + NSFW MASTER @1.5 + Nikke. Sagging returns when a slot frees.
   - **Failure-mode triage:** clothed/neutral render = filter (bypass LoRA); blur/mosaic/black = strength/quantization (back the bypass off); API error / blank placeholder = NanoGPT route-level moderation (their metadata lists Krea `nsfw: false`) — no LoRA passes that; the NSFW path is then the SI-bridge/ComfyUI profile (abliterated encoder + Enhancer patch).
   **Round-2 result (Ben, multiple tests): bypass 2-vector @5–20 + NSFW v3 @1.5 (+Nikke) STILL FAILS on NanoGPT.** Root cause, established from the bypass author's own build script (`CliffNodes/fedor_bypass/build_fedor_bypass.py`): every "bypass LoRA" is a **488-byte safetensors holding ONE tensor, `diffusion_model.txtfusion.projector.diff`, shape [1,12]** — a ComfyUI *model-diff patch* on the 12-knob text-fusion projector (cols 9/10 = −0.5117/−0.8906), NOT a rank-decomposed LoRA. A hosted diffusers-style LoRA loader (wavespeed behind NanoGPT) has no path for a `.diff` key on the projector and silently ignores it — strength 5 or 40000 changes nothing. Content LoRAs (NSFW v3 / MASTER) are real LoRAs and do load (the Nikke style LoRA visibly applies), but they sit downstream of the still-closed gate. My own base-model probes on NanoGPT show the gate's exact behaviour: "completely nude, bare breasts and nipples visible, waist-deep" → she is sunk to the collarbone so the water covers everything; "completely naked, fully out of the water, nipples, bare hips…" → full nudity renders but as a **smooth doll — no nipples, no genitals** (the "generic/censored output" the teardowns describe). That doll render is what an explicit beat looks like on NanoGPT Krea no matter the LoRA stack.
   **Conclusion: explicit anatomy on Krea 2 is ComfyUI-only** (projector diff patch via Power LoRA Loader / Krea2T Enhancer text_scale + abliterated Qwen3-VL encoder + NSFW v3 1.5). Options: **(A)** route explicit beats to the SI-bridge/ComfyUI profile running Krea 2 locally with that stack; **(B) rating-based routing** — keep NanoGPT Krea for general/suggestive beats (where it is excellent) and send explicit beats to an NSFW-capable NanoGPT model on the existing booru path (`wai-illustrious-sdxl`, `nsfw-gen-illustrious`, `hassaku-hentai`, `2dn-pony-v2`, `cyberrealistic-pony-v9` all carry `nsfw: true`; 35 of 216 NanoGPT image models do) — needs a rating signal the prose `<pic>` tag no longer carries in its prompt text (add a `rating` ATTRIBUTE, outside the prompt, and pick the profile/model from it); **(C)** accept Krea as non-explicit. Ben to rule.
   Original matrix: (a) literal anatomical prose + NSFW MASTER 1.0; (b) same + scale 1.5; (c) add Krea2 NSFW v3 (Civitai 2725430, the workflow's pick @1.5) if MASTER alone is soft; (d) if the encoder still neuters explicit text, the remaining lever is wording that the stock Qwen3-VL encodes without refusal drift (clinical/anatomical, no slang) — and failing that, the SI-bridge/ComfyUI route with the abliterated encoder + Enhancer patch. Record which rung worked.
2. **Identity consistency on prose** — the narration model re-describes characters from `visualDescriptors` each time; confirm the portrait/sprite paths (`image-portrait-generation`, `image-prompt-analysis-reference`) carry the same hair-length/style/eye/skin checklist the booru tag-bank extractor enforces, or portraits will drift between beats.
3. **Expression damping** — if grins/anger keep softening, try stating the expression twice (once in the subject clause, once in the action clause) or LoRA-side; measure.
4. **Aspect ratio**: wavespeed krea takes `resolution` 1k/2k + `aspect_ratio`; the app sends width/height and NanoGPT maps it (renders fine). If portrait/landscape framing matters for VN panels, pass `aspect_ratio` explicitly for wavespeed models.
5. **Background / portrait templates** — same density + ordering pass once the scene path is validated in play.
